<Window x:Class="PostmanCloneWPFUI.Dialogs.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prompt"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        MinWidth="380"
        Background="#0F1221"
        Foreground="#F4F6FF"
        ShowInTaskbar="False">
    <Border BorderBrush="#FF6C37" BorderThickness="0,3,0,0">
        <StackPanel Margin="22" Width="340">
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,12" Foreground="#8B93B8"/>
            <TextBox x:Name="ValueBox"
                     Padding="8,6"
                     FontSize="14"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" IsCancel="True" Padding="14,6" Margin="0,0,8,0"/>
                <Button Content="OK" IsDefault="True" Padding="18,6" Click="Ok_Click"
                        Style="{StaticResource SendButton}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
